<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.RecordWeek" parent="Theme.Material3.Dark">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorError">@color/colorError</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="colorControlNormal">@color/text_secondary</item>
        <item name="colorControlActivated">@color/accent_amber</item>
    </style>
    <style name="Theme.RecordWeek" parent="Base.Theme.RecordWeek"/>

    <!-- Celda de la cabecera de dias (L M X J V S D) de la vista mensual.
         width=0 + weight=1 -> las 7 columnas quedan del mismo ancho y coinciden
         con las celdas del calendario. Se factoriza en un style para no repetir
         estos atributos siete veces en el layout. -->
    <style name="WeekdayHeaderCell">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Tema de la pantalla de bienvenida (splash). Lo dibuja el PROPIO sistema
         mientras la app arranca; no es una Activity que esperemos a propósito.
         - windowSplashScreenBackground: color de fondo (tu azul medianoche).
         - windowSplashScreenAnimatedIcon: el icono centrado (anillo + R).
         - postSplashScreenTheme: a qué tema saltar cuando la app ya cargó.
         Hereda de Theme.SplashScreen, que aporta el comportamiento base. -->
    <style name="Theme.RecordWeek.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.RecordWeek</item>
    </style>
</resources>
