<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================
     HOST de la app. Es el MARCO FIJO que nunca se destruye al cambiar de vista.
       - fragment_container: el hueco central donde se intercambia el contenido
         (Diario / Mensual / Analisis). Ocupa todo el alto disponible (weight=1).
       - bottom_nav: la barra inferior fija con los 3 destinos principales.
     La barra SUPERIOR (titulo + engranaje de Ajustes) la pone el TEMA
     (Theme.Material3.Dark trae ActionBar) y se rellena por codigo en MainActivity.
     ============================================================ -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark">

    <!-- Hueco del contenido. height=0 + weight=1 -> se estira para ocupar todo lo
         que la barra inferior deja libre. Aqui dentro MainActivity coloca el
         Fragment activo con FragmentManager.replace(). -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- Separador fino entre el contenido y la barra, para que la barra no quede
         "pegada" visualmente al contenido sobre el fondo oscuro. -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/divider"/>

    <!-- Barra inferior de navegacion. app:menu enlaza los 3 items; itemIconTint e
         itemTextColor usan el selector de color (ambar activo / gris inactivo).
         labelVisibilityMode=labeled -> mostrar SIEMPRE el texto bajo el icono. -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface_dark"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/bottom_nav_item_color"
        app:itemTextColor="@color/bottom_nav_item_color"
        app:labelVisibilityMode="labeled"/>

</LinearLayout>
